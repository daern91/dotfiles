- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: .tmux.conf
    # ~/.vim: .vim
    ~/.gitconfig: .gitconfig
    ~/.vimrc: .vimrc
    ~/.zshrc: .zshrc
    ~/.config/alacritty/alacritty.toml: alacritty.toml
    ~/variables.sh: variables.sh

- create:
    - ~/downloads
    - ~/.vim/undodir
