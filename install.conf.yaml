- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: .tmux.conf
    # ~/.vim: .vim
    ~/.gitconfig: .gitconfig
    ~/.vimrc: .vimrc
    ~/.zshrc: .zshrc
    ~/.config/ghostty/config: ghostty.config
    ~/.config/nvim/init.lua: init.lua
    ~/variables.sh: variables.sh

- create:
    - ~/downloads
    - ~/.vim/undodir
